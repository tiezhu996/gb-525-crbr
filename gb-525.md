# gb-525 食品过敏原交叉接触矩阵

请生成英文短名为 `food-allergen-crosscontact-analyzer` 的 Go 全栈 Web 项目。系统服务食品工厂质量与配方团队，维护过敏原谱、生产路线和接触关系，通过可解释矩阵评估交叉接触风险并形成复核记录。系统只做内部风险分析，不替代法规判断、实验室检测或最终标签批准，不控制生产设备。不得实现订单、库存、采购、财务、电商或通用质量工单。

## 项目主要需求

### 1. 核心实体

- `AllergenProfile`：字段至少含 `id`、`profile_code`、`material_name`、`allergens_json`、`source_type`、`supplier_statement_date`、`profile_status`、`version`、`reviewed_by`；表示分析输入而非库存物料，贯穿全栈。
- `ProcessRoute`：字段至少含 `id`、`route_code`、`product_name`、`ordered_steps_json`、`declared_allergens_json`、`route_status`、`version`、`owner_id`；定义工艺步骤顺序和声明谱，贯穿全栈。
- `ContactEdge`：字段至少含 `id`、`from_step_code`、`to_step_code`、`contact_type`、`shared_equipment`、`cleaning_factor`、`carryover_probability`、`evidence_note`、`enabled`；构成有向接触图，贯穿全栈。
- `AssessmentRun`：字段至少含 `id`、`route_id`、`assessment_status`、`input_snapshot_json`、`matrix_json`、`risk_items_json`、`highest_risk_level`、`algorithm_version`、`created_by`、`reviewed_by`、`completed_at`；结果不可覆盖，贯穿全栈。

### 2. 核心页面

- `/profiles` 过敏原谱页：消费 `AllergenProfile`、`ProcessRoute`，维护材料谱并显示被哪些路线使用。
- `/routes` 工艺路线页：消费 `ProcessRoute`、`ContactEdge`，编辑步骤顺序和共享接触关系。
- `/matrix` 交叉接触矩阵页：消费 `ContactEdge`、`AllergenProfile`，按过敏原显示来源、传播路径和清洗衰减。
- `/assessments` 评估工作台：消费 `AssessmentRun`、`ProcessRoute`、`AllergenProfile`，运行评估、解释风险并提交复核。
- `/audit` 审计页：消费 `AssessmentRun`、`ContactEdge`，比较输入版本并检索复核事件。

### 3. 差异算法与状态流

- 从工艺路线和 `ContactEdge` 构建有向图，对每种过敏原执行带权可达性传播；每条边按 `cleaning_factor` 和 `carryover_probability` 衰减，禁止简单字符串匹配冒充矩阵算法。
- 输出来源材料、完整传播路径、累计风险分数、关键接触边和清洗证据。环路必须检测并通过最大路径长度/访问状态安全终止。
- 将风险分数映射为明确等级，但同时保留原始分数和阈值版本；声明过敏原与传播过敏原分开展示，不自动生成法规标签结论。
- `AssessmentRun` 状态为 `queued -> calculating -> pending_review -> accepted | rejected`；只有 reviewer 可接受，拒绝需原因，输入版本变化后旧结果标为 stale，不可覆盖。

### 4. 横切关注点

- JWT/RBAC 角色 `quality_analyst`、`reviewer`、`admin`，贯穿角色数据、中间件、路由、前端守卫/按钮和类型。
- 审计覆盖谱版本、路线步骤、接触边、算法阈值、评估与复核，包含 request ID、操作者、前后摘要。
- 全局错误处理统一图结构错误、版本过期、状态冲突、输入校验和权限问题；前端集中展示。
- 请求 ID、结构化日志、panic recovery、本地限流独立实现；敏感供应商备注不得进入访问日志。

### 5. 共享枚举、组件、hooks 与中间件

- `RiskLevel = low | medium | high | critical`：后端 `backend/internal/constants/risk.go`，前端 `frontend/src/types/risk.ts`。
- `AssessmentStatus = queued | calculating | pending_review | accepted | rejected | stale`：后端 `backend/internal/constants/assessment.go`，前端 `frontend/src/types/assessment.ts`。
- README 列出枚举在数据库约束、后端各层、前端 store/component/page 的全部出现位置。
- 共享组件：`RiskBadge`（矩阵/评估页）、`EvidencePathPanel`（矩阵/评估页）、`VersionDiffDialog`（路线/审计页）。
- hooks：`useAuth`、`useAssessmentPolling`，集中处理权限和运行状态。
- 中间件至少独立提供 `RequestID`、`AccessLog`、`Auth`、`RBAC`、`Recovery`、`RateLimit`。

### 6. 技术与规模红线

- Vue 3 + TypeScript + Vite + Element Plus + Pinia；矩阵和路径视图必须使用后端真实数据并支持空态/错误态。
- Go 1.22 + Gin + GORM + PostgreSQL；validator/v10、结构化日志、构造器注入、事务、`%w`、优雅停机。
- 2500-4200 行功能 Go 代码、24-42 个功能 `.go` 文件且低于 5000 行，不计测试/vendor/生成代码/前端；禁止空壳拆分或单文件大杂烩。
- API `/api/v1`，真实 `/healthz`、`/readyz`、统一响应/分页/错误码；图传播、环路、衰减和状态机需表驱动测试。
- 禁止 TODO、placeholder、伪造风险结果、handler 直连数据库和非事务复核。

### 7. 文件结构强制清单

```text
./
├── go.work                 # use ./backend
├── docker-compose.yml
├── .env
├── .env.example
├── .gitignore
├── README.md
├── runtime_smoke.json
├── backend/
│   ├── go.mod
│   ├── Dockerfile
│   ├── cmd/server/main.go
│   └── internal/{config,model,dto,repository,service,handler,router,middleware,constants,analyzer,util}/
├── frontend/
│   ├── Dockerfile
│   ├── nginx.conf
│   └── src/{api,stores,types,components/common,hooks,pages,router,utils}/
└── output/execution.md
```

四实体逐层独立文件；图构建、路径传播、评分、阈值映射各自分文件。严禁合并职责到单一文件。

根目录 `runtime_smoke.json` 只能是启动 manifest，不得保存构建、接口或 Browser 验收结果。使用下列可解析内容，并让后端支持 smoke 环境的 GORM SQLite 与 Compose 环境的 PostgreSQL：

```json
{
  "mode": "service",
  "start": "go run ./cmd/server",
  "workdir": "backend",
  "env": {
    "PORT": "20525",
    "DB_DRIVER": "sqlite",
    "DB_DSN": "file:runtime-smoke?mode=memory&cache=shared",
    "DB_AUTO_MIGRATE": "true",
    "JWT_SECRET": "runtime-smoke-gb525-jwt-secret-at-least-32-bytes"
  },
  "ready_url": "http://127.0.0.1:20525/healthz",
  "ready_status": [200],
  "startup_timeout": 30
}
```

### 8. 部署、README 与端口

- Compose 项目名 `food-allergen-crosscontact-analyzer`；前端 `18525`、后端 `19525`、PostgreSQL `57525`，容器端口 `80/8080/5432`。
- Compose 顶层 `name`、不写 `version:`，容器名使用 `${COMPOSE_PROJECT_NAME:-food-allergen-crosscontact-analyzer}` 前缀；数据库使用命名卷，数据库、后端和前端均配置 healthcheck 并按健康状态依赖，`docker compose ps` 中所有长驻服务最终必须为 healthy。
- `.env`/示例含 Compose 名、三端口、数据库变量、JWT、CORS、传播深度上限、默认阈值和日志级别。Nginx `/api` 反代并支持 SPA；中文目录可启动。
- README 中文、Docker 快速启动置顶，包含复制 env、地址、测试账号、功能、技术栈、目录、环境变量、API、枚举位置、算法说明、安全/法规边界、本地开发、排错和 License。

### 9. 强制真实验收

1. 根目录执行 `go work sync`、`go build ./backend/...`、`go vet ./backend/...`、`go test ./backend/...`；规模实测必须为 2500-4200 行、24-42 个功能 `.go` 文件且低于 5000 行；前端完成依赖安装、类型检查和 `npm run build`。
2. 执行 Compose config、构建启动、ps、健康/就绪检查，确认数据库和后端 healthy、前端可访问。
3. 至少 8 项真实接口冒烟：登录、谱创建、路线创建、接触边创建、矩阵计算、环路输入、评估提交、复核接受/拒绝、旧版本 stale 和审计查询；结果只写入 `output/execution.md`，不得写入 `runtime_smoke.json`。
4. 只用 Codex 内置 Browser 验证登录、5 页面、矩阵筛选、证据路径、评估复核和权限显隐；禁止外部 Chrome，截图文件一律保存到 `output/`，Browser 步骤与结论只写入 `output/execution.md`。
5. 执行 `docker compose down -v --remove-orphans` 并确认本项目容器为空，不影响其他项目。
6. 项目目录独立执行 `git init`，设置仓库本地身份 `git config user.name "blueship581"`、`git config user.email "brysj.hhrhl.g@gmail.com"`。先提交除 `output/` 验证证据外的实现文件并取得实现 commit hash，将 hash 写入 `output/execution.md`，再把 `output/execution.md` 与截图作为第二次报告提交；最终 `git status --short` 为空。
